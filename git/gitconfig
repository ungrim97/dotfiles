[user]
	name = Mike Francis
	email = ungrim97@gmail.com
[core]
	editor = vim
[merge]
	tool = vimdiff
[alias]
    gl  = log --colour --graph --pretty=format:'%Cred%h%Creset -%C(auto)%d%Creset %s %Cgreen(%cr) %C(magenta)(%ar) %C(bold blue)<%an>%Creset' --abbrev-commit --date=relative
    sur = submodule update --recursive
    co	= checkout
    st	= status
    cp  = cherry-pick
[push]
    default = upstream
